use strict;
use warnings;

my %info = (
	name => "lizl",
	age  => 33,
	city => "Beijing",
);
=comment
1. within each pair, use the big arrow; between pairs, use comma. 
2. use the trailing separator for future expansion.
=cut

# see Boolean_return_value.pl for more
