# $hash{EXPR}
# the value of EXPR is a string
$temp_hash{$self->private_invoke("stc::get $_ -Location")}
$books{$person}
$family_name{ $foo . "ney" }
$option_table{lc($user_option)}
$genres{-$key}

# $hash{bareword}
# physical bareword, can omit quote marks
$ENV{JT_LOG_DIR}
